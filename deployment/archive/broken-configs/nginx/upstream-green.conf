# Green environment upstream configuration
# This file defines the backend and frontend servers for the GREEN environment

upstream backend {
    server api-green:5000;
}

upstream frontend {
    server frontend-green:8080;
}
