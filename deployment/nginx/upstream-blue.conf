# Blue environment upstream configuration
# This file defines the backend and frontend servers for the BLUE environment

upstream backend {
    server api-blue:5000;
}

upstream frontend {
    server frontend-blue:8080;
}
